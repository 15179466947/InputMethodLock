# AGENTS.md — AI 编码代理工作约定

本文件约束在本仓库工作的 AI 编码代理（Codex / Claude / ZCode 等）。两条核心纪律，
违反即视为交付不合格。

---

## 纪律一：Git 提交纪律

### 大修改：完成后必须主动提交

以下情况属于**大修改**，完成（编译通过）后必须立即 `git commit` 并 `git push` 到 `origin/main`：

- 新增、删除或重构功能模块（`src/` 下任何行为变更）
- 修复 bug（无论大小，只要改变了运行行为）
- 修改构建/打包脚本（`build.cmd`、`build-release.cmd`、`packaging/`）
- 批量文档更新（新版本更新日志、方案变更）
- 依赖资源变更（图标、清单、语言包等）

提交信息格式（参考既有提交风格）：

```
v<版本号或主题>: <一句话概括>

- 要点 1
- 要点 2
```

### 小修改：先询问用户，不要擅自提交

以下情况属于**小修改**：错别字、注释/日志文案调整、格式微调、不影响行为的重命名等。
处理方式：**改完后明确询问用户"是否需要提交"**，得到确认后再提交。

### 禁止事项

- 禁止把多个不相关改动混进同一个提交
- 禁止提交编译不通过的代码
- 禁止使用 `git commit --no-verify` 或绕过任何检查

### 注意事项

- 提交远程仓库需要和用户确认
- 本地提交的话存在需求、bug等修改就可以提交
- 涉及到设计方案上的修改需要同步更新设计方案，如有需要更新Readme也需要同步更新
- 更新日志需要同步更新

---

## 纪律二：交付前必须跑通验证

**杜绝"写完就交差"式交付。** 每次向用户报告"完成"之前，必须完成对应级别的验证：

### 最低门槛（所有交付）

- `build.cmd` 编译通过，无 error（warning 需说明）
- 如果改了打包相关内容：`build-release.cmd` 跑通，`dist/` 下能产出安装包
- 如果有远程仓库：已 push 成功

### 功能交付（涉及行为变更）

本项目**没有自动化测试**，输入法行为只能在真机上验证。因此：

1. 交付说明中必须包含两个清单，明确区分：
   - **已验证项**：编译通过、日志/静态走查确认的逻辑路径
   - **待实测项**：需要用户在真机上执行的具体测试步骤（操作 → 预期结果）
2. 涉及输入法行为的改动，必须给出针对性测试步骤，例如：
   - 三种模式（英文锁定 / 中文锁定 / 布局锁定）逐一验证
   - 微软拼音与第三方输入法（搜狗等）分别验证
   - 热键、托盘左键开关、例外程序、解锁恢复
3. 用户反馈"无效/有问题"时，优先取 `debug.log` 定位，不要凭猜测再改一版

### 禁止事项

- 禁止说"应该可以了""理论上没问题"这类未验证的结论
- 禁止把"待实测项"说成"已验证"
- 禁止跳过编译直接交付源码改动
